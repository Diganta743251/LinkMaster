package com.example.link

// This file is kept for backward compatibility
// The app now uses Jetpack Compose instead of RecyclerView
// All UI is handled in MainActivity.kt with Compose LazyColumn

@Deprecated("Use Compose LazyColumn instead")
class LinkAdapter {
    // Stub class - functionality moved to Compose
}