package fe.linksheet.module.viewmodel

import androidx.lifecycle.ViewModel

class MainViewModel : ViewModel() {
    fun enqueueNavEvent(destination: Any?, args: Any?) {
        // Minimal no-op for compile-only build
    }
}
